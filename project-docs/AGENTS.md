# AI AGENTS (roles)

Use ONE role per request.

## Architect
Decides structure, boundaries, and avoids scope creep.

## DB
Owns Prisma schema + migrations.

## Backend
Owns business logic + API routes + auth checks.

## Frontend
Owns pages, forms, tables, UX.

## QA
Owns test checklist + edge cases + manual test steps.
